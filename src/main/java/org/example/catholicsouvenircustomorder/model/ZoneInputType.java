package org.example.catholicsouvenircustomorder.model;

public enum ZoneInputType {
    TEXT,      // Nhập text tự do
    COLOR,     // Chọn màu
    SELECT,    // Chọn từ danh sách
    IMAGE      // Upload ảnh
}
